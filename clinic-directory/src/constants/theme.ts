/**
 * Design System - Unified Theme Export
 * دليل العيادات - Clinic Directory
 */

import { Colors } from './colors';
import { Typography, FontSize, FontWeight, FontFamily } from './typography';
import { Spacing, BorderRadius, Shadow, Layout } from './spacing';

export const Theme = {
  colors: Colors,
  typography: Typography,
  fontSize: FontSize,
  fontWeight: FontWeight,
  fontFamily: FontFamily,
  spacing: Spacing,
  borderRadius: BorderRadius,
  shadow: Shadow,
  layout: Layout,
};

export { Colors, Typography, FontSize, FontWeight, FontFamily, Spacing, BorderRadius, Shadow, Layout };
export default Theme;
